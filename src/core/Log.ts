interface LogOptions {
    logDir: string
}

export class Log {
    static constance = '';
    logOption = undefined;
    constructor(logOpt: LogOptions)
    constructor() {
        // if (arguments[0] instanceof LogOptions)



    }
}